nota = float(input('Informa a primeira nota: '))
nota2 = float(input('Informa a segunda nota: '))

media = (nota + nota2) / 2

print('A media entre {:.1f} e {:.1f} é igual a {:.1f}'.format(
    nota, nota2, media))
